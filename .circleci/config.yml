version: 2
jobs:
  build:
    docker:
      - image: zoobab/openwrt-x86-attitude:latest
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Get Interactive prompt
          command: /bin/ash      
